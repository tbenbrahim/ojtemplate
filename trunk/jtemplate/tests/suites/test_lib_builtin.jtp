var testSuite={
	description: 'System Library',
	tests: [
		{
        	description: 'System.command',
        	test: function(){
        		return System.command('whoami')==0;
        	}
        },{
        	description: 'array push',
        	test: function(){
        		let a=[1,2,'3'];
        		Array.push(a,'4');
        		Array.push(a,5);
        		return a==[1,2,'3','4',5];
        	}
        },{
        	description: 'array pop',
        	test: function(){
        		let arr=[1,2,'3','4',5];
        		let a=Array.pop(arr);
        		let b=Array.pop(arr);
        		return arr==[1,2,'3'] && a==5 && b=='4';
        	}
        },{
        	description: 'length',
        	test: function(){
        		let arr1=[1,2,'3','4',5];
        		let arr2=[];
        		return Array.length(arr1)==5 &&
        		       Array.length(arr2)==0 &&
        		       Array.length([1,2,'3','4',5])==5;
        	}
        },
	]
};