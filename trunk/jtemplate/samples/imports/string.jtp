/***
Extensions to the String map
***/

//try to create a circular reference!
import 'date.jtp';

var String.prototype.padLeft=function(len,pad){
    var result=this+'';
    while (result.length()<len){
        result=pad+result;
    }
    return result;
};