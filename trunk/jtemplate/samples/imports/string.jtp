/***
Extensions to the String map
***/

//try to create a circular reference!
import 'date.jtp';

var String.padLeft=function(inp,len,pad){
    var result=inp+'';
    while (String.length(result)<len){
        result=pad+result;
    }
    return result;
};