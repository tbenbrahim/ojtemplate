let multiplicationSign=function(a,b){
	if (a==0 || b==0)
		return 0;
	else
		if ((a>0 && b>0) || (a<0 && b<0))
			return 1;
		else if ((a>0 && b<0)||(a<0 && b>0))
			return -1;
};

println('This statement will not be executed when the file is imported');