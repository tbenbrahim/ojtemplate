// Example 1
let side=4;

let square=function(x){
	return x*x;
};

let printarea=function(x){
	println(square(x));
};

printarea(side);

// Example 2

let add=function(x,y){
	return x+y;
};

let printf=function(f,x,y){
	println(f(x,y));
};

let a=1;
let b=2;

printf(add,a,b);
